local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")

local DASH_REMOTE_NAME = "Forward Dash Attack"
local HITBOX_MULT = 2
local LOCK_OFFSET = Vector3.new(0,2,0)

local lastHitTarget = nil
local LOCK_ENABLED = false
local currentRoot = nil
local isDashing = false

local ScreenGui = Instance.new("ScreenGui", LocalPlayer:WaitForChild("PlayerGui"))
ScreenGui.ResetOnSpawn = false
local StatusBox = Instance.new("Frame", ScreenGui)
StatusBox.Size = UDim2.new(0,20,0,20)
StatusBox.Position = UDim2.new(1,-30,1,-30)
StatusBox.BackgroundColor3 = Color3.fromRGB(255,0,0)

local function updateGUI()
    StatusBox.BackgroundColor3 = LOCK_ENABLED and Color3.fromRGB(0,255,0) or Color3.fromRGB(255,0,0)
end

local function updateLastHit(target)
    lastHitTarget = target
end

local function applyBuff(target)
    if not target or not target:FindFirstChild("HumanoidRootPart") then return end
    local part = target:FindFirstChild("HumanoidRootPart")
    part.Size = part.Size * HITBOX_MULT
end

local function dashTowards(target)
    if not target or not currentRoot then return end
    local startPos = currentRoot.Position
    local targetPos = Vector3.new(target.Position.X, target.Position.Y - 2, target.Position.Z)
    local alpha = 0
    local dashDuration = 0.2
    while alpha < 1 do
        alpha = alpha + RunService.RenderStepped:Wait()/dashDuration
        local newPos = startPos:Lerp(targetPos, math.clamp(alpha,0,1))
        currentRoot.CFrame = CFrame.new(newPos, Vector3.new(target.Position.X, newPos.Y, target.Position.Z))
    end
end

UIS.InputBegan:Connect(function(input, gpe)
    if gpe then return end
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        local mouse = LocalPlayer:GetMouse()
        if mouse.Target and mouse.Target.Parent then
            if Players:FindFirstChild(mouse.Target.Parent.Name) then
                updateLastHit(mouse.Target.Parent)
            end
        end
    elseif input.KeyCode == Enum.KeyCode.R then
        LOCK_ENABLED = not LOCK_ENABLED
        updateGUI()
    end
end)

for _, obj in ipairs(ReplicatedStorage:GetDescendants()) do
    if obj:IsA("RemoteEvent") and obj.Name == DASH_REMOTE_NAME then
        obj.OnClientEvent:Connect(function()
            if LOCK_ENABLED and lastHitTarget and currentRoot then
                local targetRoot = lastHitTarget:FindFirstChild("HumanoidRootPart")
                if targetRoot and targetRoot.Position.Y > currentRoot.Position.Y + 1 then
                    isDashing = true
                    applyBuff(lastHitTarget)
                    dashTowards(targetRoot)
                    isDashing = false
                end
            end
        end)
    end
end

local function onCharacterAdded(char)
    currentRoot = char:WaitForChild("HumanoidRootPart")
end

if LocalPlayer.Character then
    onCharacterAdded(LocalPlayer.Character)
end
LocalPlayer.CharacterAdded:Connect(onCharacterAdded)
updateGUI()
